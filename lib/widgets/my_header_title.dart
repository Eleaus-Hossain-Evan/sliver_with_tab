import 'package:flutter/material.dart';

class MyHeaderTitle extends StatelessWidget {
  const MyHeaderTitle({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
